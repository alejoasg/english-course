import { CombineDirective } from './combine.directive';

describe('CombineDirective', () => {
  it('should create an instance', () => {
    const directive = new CombineDirective();
    expect(directive).toBeTruthy();
  });
});
